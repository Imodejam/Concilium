{
  "name": "concilium",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace=@concilium/shared",
    "dev": "concurrently -n api,web,bot \"npm run dev --workspace=@concilium/api\" \"npm run dev --workspace=@concilium/web\" \"npm run dev --workspace=@concilium/bot\"",
    "dev:api": "npm run dev --workspace=@concilium/api",
    "dev:web": "npm run dev --workspace=@concilium/web",
    "dev:bot": "npm run dev --workspace=@concilium/bot",
    "type-check": "tsc -b"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.6.0"
  }
}
